# 单词记忆 H5 App — 最终定稿需求

## 项目定位

基于魔神遗忘曲线模型的轻量级 H5 单词记忆工具。

### 核心原则
- H5 网页应用，手机浏览器直接打开，不须安装
- PWA 离线可用，无网络也能背
- 零后台，数据存本地 IndexedDB
- 可导入词库（四级、高中、考研等）

## 核心流程

每日上限 N 个（默认 100，可调），新词 + 复习共用额度。

```
每日开始 → 先学新词 → 再复习到期旧词
         → 复习积压则次日优先复习
         → 新词额度未用完 → 自动补给复习
```

### 关键规则
1. 新词额度未用尽 → 补给复习，不浪费
2. **积压复习优先级永远高于新词**，杜绝越学越欠账
3. 支持手动切换模式：仅学新词 / 仅复习 / 智能混合
4. T5 通关标记为已掌握，永久不弹
5. 新词支持「暂时跳过」→ 不入记忆库、不占额度

## 两种模式

### 浏览模式（新词）
- 展示：英文 + 中文释义 + 音标 + 例句，一次性全展示
- 看一遍过，不翻转
- 看完后自动排入 T0

### 复习模式（旧词按遗忘曲线到期）
- 先看英文 → 回忆中文 → 翻转看答案
- 自评：
  - ✅ **记住了** → 升阶，间隔拉长
  - 🔄 **模糊** → **降阶，间隔减半**
  - ❌ **没记住** → 归零 T0

## 遗忘曲线

| 阶段 | 间隔 | 说明 |
|------|------|------|
| T0 | 初次记忆 | 看完排入 |
| T1 | 1小时 | T0通过 |
| T2 | 1天 | T1通过 |
| T3 | 7天 | T2通过 |
| T4 | 30天 | T3通过 |
| T5 | 365天 | T4通过 → 已掌握 |

### 统一算法
- 通过 = 升阶，间隔拉长
- 模糊 = 降阶，间隔减半
- 遗忘 = 归零 T0
- T5 完成 = 标记已掌握，退出复习队列

## 词库导入

TXT / CSV 格式：`word, meaning, phonetic, example`
- 自动过滤空行、重复单词、格式错乱行
- 重复单词以最新导入覆盖去重

## 数据结构（IndexedDB）

```json
{
  "word": "abandon",
  "phonetic": "/əˈbændən/",
  "meaning": "放弃；遗弃",
  "example": "They had to abandon the project.",
  "currentStage": 0,
  "nextReviewTime": "2026-07-18T14:00:00",
  "lastStudyTime": "2026-07-18T13:00:00",
  "status": "learning",
  "failCount": 0
}
```

支持导入/导出 JSON 备份恢复。

## UI 设计

```
┌──────────────┐
│   📚 词库     │  ← 顶栏
│              │
│  今日复习: 23  │
│  新词: 10     │
│              │
│  ┌──────────┐│
│  │  abandon  ││  ← 单词卡片（大号英文）
│  │           ││
│  │ /əˈbændən/││  ← 音标
│  │           ││
│  │ 放弃；遗弃 ││  ← 中文释义（翻转后显示）
│  │           ││
│  │ They had  ││  ← 例句
│  │ to abandon││
│  │ the pro.. ││
│  │           ││
│  └──────────┘│
│              │
│  忘记  模糊  记住 │  ← 底栏三按钮
│              │
│  进度: 12/23  │  ← 进度条
└──────────────┘
```

### 配色
- 记住：绿色
- 模糊：黄色
- 忘记：红色
- 页面：浅蓝/白底，护眼模式

## 新增功能

- 统计面板：今日学习/复习数、累计掌握、待复习
- 薄弱单词本：多次遗忘自动归集，可单独强化复习
- 每日 0 点自动刷新额度
- 卡片翻转动画
- 页面状态缓存（刷新不丢进度）
- 防重复提交

## 技术方案

- 纯前端 H5，零后端
- 纯 HTML + CSS + JavaScript 或 Vue 3 + Vite
- IndexedDB 存储
- PWA Service Worker 离线缓存
- 词库导入/导出备份
- 可部署到 GitHub Pages

## 开发排期

| 阶段 | 内容 | 预估 |
|------|------|------|
| P0 | 核心卡片 + 遗忘曲线算法 + IndexedDB | 先做 |
| P1 | 词库导入导出 + 内置 CET-4 词库 | +1天 |
| P2 | UI 美化 + PWA + 统计面板 | +2天 |
